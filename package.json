{
  "name": "speedtest-gaming-analyzer",
  "version": "2.0.0-optimized",
  "description": "Web speed test untuk estimasi download/upload, latency, jitter, packet loss, region game, dan Steam/CDN",
  "private": true,
  "scripts": {
    "start": "node speedtest-server.js",
    "speedtest": "node speedtest-server.js"
  },
  "dependencies": {
    "@m-lab/ndt7": "^0.1.4"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
